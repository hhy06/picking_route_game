# AI拣货游戏开发计划

## 1. 技术选型

### 后端
- **Web框架**: Flask (轻量、简单)
- **TSP求解**: elkai (Python TSP求解库)
- **数据**: 纯Python dict/json

### 前端 (暂不实现)
- Vue3 + Vite

## 2. 仓库地图结构

```
参数:
- a: 行数 = 2a+1 (奇数行)
- k: 每行货架段数
- b: 每段货架格子数

列数 = k*(b+1)+1

空格点 (可通行):
  - 行坐标: 偶数行 (0, 2, 4, ...)
  - 列坐标: col % (b+1) == 0 的位置 (即走廊)

货架点 (不可通行):
  - 其余位置

起点: 左下角空格点 (行=2a, col=0)
```

## 3. 订单数据结构

```python
order = {
    "order_id": "xxx",
    "skus": [
        {"sku_id": "A1", "row": r, "col": c},  # 货架位置(奇数行,不可整除的列)
        ...
    ]
}
```

## 4. 拣货路线格式

```python
route = [
    [row, col],  # 起点
    [row, col],  # 第1个SKU
    [row, col],  # 第2个SKU
    ...
    [row, col]   # 返回起点
]
```

## 5. API接口

### POST /api/generate_order
生成随机订单
- 输入: `{"a": 3, "k": 4, "b": 3}` (可选)
- 输出: `{"success": true, "order": {...}, "map_params": {...}}`

### POST /api/solve
求解TSP得到AI最优路线
- 输入: `{"map": [[0,1,...], ...], "start": [r,c], "middle_points": [[r,c], ...]}`
- 输出: `{"success": true, "route": [...], "total_distance": 10}`

### POST /api/validate_route
验证人类路线是否合法
- 输入: `{"map": [...], "route": [...],"order_skus": [...]}`
- 输出: `{"valid": true/false, "distance": 10, "error": "..."}`

### GET /api/map_info
获取当前地图信息
- 输出: `{"rows": 7, "cols": 13, "walkable": [[r,c],...]}`

## 6. 算法模块

### shortest_path.py
BFS求两点间最短路径
- `shortest_path_sequence(grid_map, start_coord, end_coord) -> list[coord]`

### tsp_solver.py
求解TSP得到最优回路
- `shorted_round_trip(grid_map, start_coord, middle_points_list) -> list[coord]`
- 内部使用elkai库

## 7. 依赖

```
Flask
elkai
numpy
```

## 8. 项目目录

```
picking_route_game_website3/
├── backend/
│   ├── __init__.py
│   ├── app.py              # Flask服务器
│   ├── shortest_path.py    # 算法A
│   ├── tsp_solver.py       # 算法B
│   ├── data_generator.py   # 订单生成器
│   └── constants.py        # 常量定义
├── frontend/               # 暂不实现
├── tests/
│   └── test_backend.py
├── start.py                # 启动脚本
└── requirements.txt
```
